Create a shell script of OSX/WSL/Linux and a batch script that can create an alias for the current user with a new aliases in their profile.

'dubu' changes to the repo path root and opens claude there.

'dubuhome' changes to their home folder from the CLAUDE.local.md.

Before putting these aliases in with a comment regarding what they do, have the script check if its already there and report to the user to overwrite the alias if it already exists.

Update the README.md in root and the integrations\README.md